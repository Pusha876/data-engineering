[project]
name = "pipeline"
version = "0.1.0" 
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "pyarrow",
    "fastparquet",
    "sqlalchemy>=2.0.49",
    "psycopg[binary,pool]>=3.2.13",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pgcli>=4.4.0",
]
